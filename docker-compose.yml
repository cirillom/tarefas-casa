services:
  tarefas-casa:
    build: .
    container_name: tarefas-casa-dev
    restart: unless-stopped

    ports:
      - "8000:8000"

    environment:
      - TZ=America/Sao_Paulo

    volumes:
      - ./data-dev:/data
